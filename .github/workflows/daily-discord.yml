name: Daily Recruitment Summary

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  discord:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: |
          echo "Daily recruitment summary"
